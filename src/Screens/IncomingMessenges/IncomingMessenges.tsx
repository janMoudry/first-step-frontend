import { View } from "react-native";
import { colors } from "../../Theme/Theme";

const IncomingMessenges = () => (
  <View
    style={{
      width: "100%",
      height: "100%",
      backgroundColor: colors.backgroundMain,
    }}
  />
);

export default IncomingMessenges;
